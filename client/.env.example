# Client environment example (Vite)
#
# Note: This project uses relative API calls (axios baseURL: "/api") and a Vite dev proxy.
# VITE_API_URL is documented for deployments, but may not be used by the runtime code.

# Intended base URL for API requests (optional; documentation-ready)
VITE_API_URL=http://localhost:5000

